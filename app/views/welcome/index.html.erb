
<div class="container" >
<h1>Welcome Git demo#index</h1>
<p>Find me in app/views/welcome/index.html.erb</p>
</div>
<div class="container-fluid">
  <div class="row">
    <div class="col-md-3">
    </div>
    <div class="col-md-8">
      <%= link_to 'New post', new_post_path %>
      <div class="row" >
      <ul>
      <% @posts.each do |post| %>

      <h3><%= post.user.name %> </h3>
      <h4><%= post.title %> </h4>
      <div class="module" >
      <%= image_tag post.image.url , style: 'height:auto;width:80%;' %>
      </div>
      <p><%= post.text %></p>
      <p> <br><%= link_to 'Show', post %> 
      </tr>
      <% end %>
      </ul>
    </div>
  </div>
</div>